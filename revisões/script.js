const buttons = document.querySelectorAll(".btt");

buttons.forEach(element => {
    element.addEventListener("click", (e) => {
        const styles = e.currentTarget.classList;
        if (styles.contains("back")) {
            //
        } else if (styles.contains("back")) {
            //
        } else if (styles.contains("back")) {
            //
        }
    })
});